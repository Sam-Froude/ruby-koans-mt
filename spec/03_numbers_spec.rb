require "rspec"

# The Ruby spirits are counting on you.

RSpec.describe "Numbers: " do

  # replace ___ with the correct answer to make the tests pass

  it "Are integers the same as floating points?" do
    expect(1).to eq(1.0)
  end

end

åå<<-DOC
  int equals to float,
    addition,
    subtraction,
    multiplication, 
    division,

    +=,
    -=,
    *=,
    /=,

    exponents
    remainder,
    modulus,
    rounding(ceil, floor, abs, round),
    truncate,
    odd,
    even,
    next,
    pred,
    to_i,
    to_s,
    .integer? (check if integer)
    .digits,
    range (1..10)
DOC